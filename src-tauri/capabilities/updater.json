{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-updater",
  "description": "User-initiated signed reader updates on Windows",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": ["updater:default", "core:resources:allow-close"]
}
