{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local library access for the application window; no remote origins.",
  "windows": ["main"],
  "permissions": ["sql:allow-load", "sql:allow-select", "sql:allow-execute"]
}
